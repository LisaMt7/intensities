import { GraphWorkspace } from '../Workspace';
import { GraphNode } from '../Node';
declare const dragElement: (workspace: GraphWorkspace, dragItem: GraphNode, onMove: any, onDown: any, onUp: any) => void;
export default dragElement;
